<%- include("../_partials/header")%>


<!-- <h1><%= userShow.name %></h1>
<div>
    <img src="<%= userShow.avatar %>">
</div>
<h3><%= userShow.name%> 's Favorites</h3>

<ul>
  <% userShow.favorites.forEach(function(favorite) { %>
        <li><%= favorite.title %></li>
        <div><img class="favorite-movie" src="https://image.tmdb.org/t/p/w500/<%= favorite.poster %>"</div>
        <form method="POST" action="/movies/favorites/<%= favorite._id %>?_method=DELETE">
            <input type="submit" class="btn btn-danger" value="Delete Favorite">
        </form>
  <% }); %>
</ul> -->


<div class="container-fluid">
    <div class="row content">
        <div class="col-sm-3 sidenav">
            <h4><%= userShow.name %></h4>
            <div class="well">
                <img src="<%= userShow.avatar %>">
            </div>
            <br>
        </div>
      
        <div class="col-sm-9">
            <h2><small><%= userShow.name%>'s Favorites</small></h2>
            <hr>
            <div class="row mb-2">
                <% userShow.favorites.forEach(function(favorite) { %>               
                        <div class="col-md-4">
                            <div class="card mb-4 box-shadow">
                                <img class="card-img-top" src="https://image.tmdb.org/t/p/w500/<%= favorite.poster %>" alt="Card image cap">
                                <div class="card-body">
                                    <p class="card-text text-truncate"><%= favorite.title %></p>
                                    <hr>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="btn-group">
                                            <a href="" class="btn btn-sm btn-outline-secondary">View</a>
                                            <form method="POST" action="/movies/favorites/<%= favorite._id %>?_method=DELETE">
                                                <input type="submit" class="btn btn-sm btn-outline-secondary" value="Delete Favorite">
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    
                    <% }); %>
            </div>
        </div>
    </div>
</div> 


<%- include("../_partials/footer")%>
